{
    "name": "Circuit.Heisenberg_Ansatz", 
    "kind": "ansatz",
    "description": "Variational ansatz specialized for Heisenberg model",
    "tags": ["heisenberg", "ansatz", "variational", "hardware_efficient"],
    "needs": [],
    "provides": ["ansatz"],
    "yields": {
        "ansatz": "QuantumCircuit ansatz"
    },
    "params_schema": {
        "n": {
            "type": "int",
            "description": "Number of qubits",
            "default": 4,
            "range": [2, 20]
        },
        "reps": {
            "type": "int", 
            "description": "Number of ansatz repetition layers",
            "default": 1,
            "range": [1, 10]
        }
    },
    "helper_function": "heisenberg_ansatz",
    "invoke_template": "{var}_ansatz = heisenberg_ansatz(n, reps)",
    "codegen_hint": {
        "cell_name": "circuit_heisenberg",
        "import": "from qiskit import QuantumCircuit; from qiskit.circuit import ParameterVector"
    }
}